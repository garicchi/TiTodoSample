/*describe('Android browser.js', function(){
	var browserjs = path.join(__dirname, 'Resources', 'android', 'alloy', 'controllers', 'browser.js'),
		context = slag(browserjs, {
			titanium: TITANIUM,
			alloy: ALLOY,
			platform: 'android',
			module: {
				Alloy: Alloy.android
			}
		});

	it('should does not throw exception in the main Controller', function(){
		assert.doesNotThrow(function(){
			context.Controller();
		});
	});

	it('should does not throw exception in the doRender', function(){
		assert.doesNotThrow(function(){
			context.doRender();
		});
	});
});
*/